import 'package:flutter/material.dart';

class TransaksiView extends StatelessWidget {
  const TransaksiView({super.key});

  @override
  Widget build(BuildContext context) {
    return Text("halaman Transaksi");
  }
}